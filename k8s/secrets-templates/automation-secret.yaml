apiVersion: v1
kind: Secret
metadata:
  name: retrogemini-automation
type: Opaque
stringData:
  # Optional: owner/repo for repository_dispatch target
  FEEDBACK_AUTOMATION_GITHUB_REPO: ""
  # Optional: GitHub token with permission to trigger repository_dispatch
  FEEDBACK_AUTOMATION_GITHUB_TOKEN: ""
