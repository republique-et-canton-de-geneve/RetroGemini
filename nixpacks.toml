# Nixpacks configuration for Railway
# Alternative to Dockerfile - Railway auto-detects and builds

[phases.setup]
nixPkgs = ["nodejs_22", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview"

[variables]
NODE_ENV = "production"
